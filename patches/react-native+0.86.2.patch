diff --git a/node_modules/react-native/Libraries/Components/TextInput/TextInput.js b/node_modules/react-native/Libraries/Components/TextInput/TextInput.js
index adca4e3..ac333e3 100644
--- a/node_modules/react-native/Libraries/Components/TextInput/TextInput.js
+++ b/node_modules/react-native/Libraries/Components/TextInput/TextInput.js
@@ -354,7 +354,14 @@ function useTextInputStateSynchronization({
  * or control this param programmatically with native code.
  *
  */
-function InternalTextInput(props: TextInputProps): React.Node {
+function InternalTextInput(rawProps: TextInputProps): React.Node {
+  // Cap system font scaling (e.g. an accessibility "font size: 200%" setting)
+  // so it can't blow out layouts tuned for ~100%, while still allowing some
+  // scale-up. Callers can still opt into a different value explicitly.
+  const props: TextInputProps =
+    rawProps.maxFontSizeMultiplier == null
+      ? {...rawProps, maxFontSizeMultiplier: 1.3}
+      : rawProps;
   const {
     'aria-busy': ariaBusy,
     'aria-checked': ariaChecked,
diff --git a/node_modules/react-native/Libraries/Text/Text.js b/node_modules/react-native/Libraries/Text/Text.js
index 8e7b29c..8d5ec61 100644
--- a/node_modules/react-native/Libraries/Text/Text.js
+++ b/node_modules/react-native/Libraries/Text/Text.js
@@ -88,6 +88,12 @@ const TextImpl: component(
   const processedProps = restProps as {
     ...NativeTextProps,
   };
+  // Cap system font scaling (e.g. an accessibility "font size: 200%" setting)
+  // so it can't blow out layouts tuned for ~100%, while still allowing some
+  // scale-up. Callers can still opt into a different value explicitly.
+  if (processedProps.maxFontSizeMultiplier == null) {
+    processedProps.maxFontSizeMultiplier = 1.3;
+  }
   const _accessibilityLabel = ariaLabel ?? accessibilityLabel;
   let _accessibilityState: ?TextProps['accessibilityState'] =
     accessibilityState;
